# David's blog

Static GitHub Pages version of David's blog.

## Build

```bash
npm run build
```

The blog content lives in `data/blog.json`. The build script generates:

- `index.html`
- `topics/<topic>/index.html`
- `posts/<slug>/index.html`
- `styles.css`
